#include<bits/stdc++.h>
#include <cmath>
using namespace std;
int main()
{
    int l,r;
    string s;
    cin>>l>>r;
    cin>>s;
    while(l<=r)
    {
        swap(s[l-1],s[r-1]);
        l++;
        r--;
    }
cout<<s<<endl;
    return 0;
}

